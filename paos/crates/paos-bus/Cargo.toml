[package]
name = "paos-bus"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
paos-store = { path = "../paos-store" }
rusqlite.workspace = true

# Tests only: the read-only layer must agree with the writer that produced the rows, so
# the presence writer seeds the fixtures rather than hand-rolled SQL that could drift.
[dev-dependencies]
paos-presence = { path = "../paos-presence" }
